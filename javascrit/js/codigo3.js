
// Utilizar let const (var NO)
    // let number = 25;
    // alert(number);

// Underfined: Significa que la variable existe en el programa pero notiene valor
// null: Siginifica que el valor es vacio. ya la inicializamos y le decimos que es vacia
    // let x = null;
    // alert(x);

// promt es una api, es para que en un mensaje puedas añadir algun mensaje
    // let nombre = prompt("Inserte algun numero:");
    // alert("hola" + nombre);

//Operadores de asignacion---
    // let numero = 10;
    // numero += 5; = numero = numero +5;
    // numero %= 5; REST0 de la division
    // numero **=5; exponenciacion CUBO
    // document.write(numero);  esto lo copia en el docuemento

//Concantenar---
    // num1 = "5";
    // num2 = 9;
    //Con el concat debe haber SI o SI un string
    // frase = num1.concat(num2);

    // frase = "" + num1 + num2;
    // document.write(frase);

// Concatenar un String---
    // nombre = "Marithza"
    // frase = "Soy " + nombre +" estoy caminando";

    // Para que funcione ${} esto debe tener el texto esto ``
    // frase = `Soy ${nombre} estoy caminando`;
    // ctrl + alt + } Para colocar ``

    // document.write(frase);

    // ESTO ES UN ERROR 
    //frase = "Soy " nombre" estoy caminando";
    // SI QUEREMOS UILIZAR COMILLAS DOBLES DEBEMOS PONER AFUERA''
    //frase = 'Soy " nombre" estoy caminando';

// Operadores de compararcion---
     let num = 23;
     let num2 = 13;

     // == Si un numero es igual COMPARA
     document.write(num == num2);
     // = ASIGNA no compara
     // != DISTINTO
     // === EXTRICTAMENTE IGUALES


//Oreadores---









